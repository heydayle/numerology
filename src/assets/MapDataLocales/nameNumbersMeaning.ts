import { name_number_meaning_map, character_number_meaning_map } from "../locales/vietnamese";

export const nameNumbersMeanings = new Map<string, any>([
    ['vi', name_number_meaning_map]
])

export const characterNumberMeanings = new Map<string, any>([
    ['vi', character_number_meaning_map]
])